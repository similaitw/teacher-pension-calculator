{
  "name": "teacher-pension-calculator",
  "version": "2.0.0-rc.1",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test",
    "test:official": "node scripts/validate-official-cases.js",
    "test:release": "npm test && npm run test:e2e",
    "test:formal-release": "npm run test:release && npm run test:official"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@playwright/test": "^1.55.0",
    "http-server": "^14.1.1"
  }
}
